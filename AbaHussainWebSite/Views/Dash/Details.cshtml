@model System.Data.DataTable
@{
    ViewBag.Title = "تفاصيل الفرع";
    Layout = "~/Views/_LayoutPage1.cshtml";
}
@if (ViewBag.cunt > 0)
{
<div class="containertbl" dir="rtl">
    <h2 style="text-align:right">
        فرع :
        @*@Html.EditorFor(model => model.Rows[0]["Title"], new { htmlAttributes = new { @class = "form-control" } })
        @Html.EditorFor(model => model.Rows[0]["enTitle"], new { htmlAttributes = new { @class = "form-control" } })*@
        @Model.Rows[1]["Title"]
        @Model.Rows[1]["enTitle"]
    </h2>
    <h3 style="text-align:right">
        <span> التليفون : </span>
        @Model.Rows[0]["phone"]
        @*@Html.EditorFor(model => model.Rows[0]["phone"], new { htmlAttributes = new { @class = "form-control" } })*@

    </h3>
   <p style="text-align:right">
       العنوان :
       @Model.Rows[1]["AddBranche"]
       @Model.Rows[1]["enAddrBranch"]
         @*  @Html.EditorFor(model => model.Rows[0]["AddBranche"], new { htmlAttributes = new { @class = "form-control" } })*@
           @*@Html.EditorFor(model => model.Rows[0]["enAddrBranch"], new { htmlAttributes = new { @class = "form-control" } })*@

   </p>
    <img style="text-align:right" src="@Url.Content(Model.Rows[1]["imgSrc"].ToString())" width="200" height="100" alt="error"/>
    <hr/>
    
    <table class="table table-hover" dir="rtl">
        <tr>
            <th>
               الصور الفرعيه للفرع
            </th>
            <th>
                
            </th>
        </tr>

        @for (int i = 0; i < Model.Rows.Count; i++)
        {
            <tr>
               
                <td>
                    <img src="@Url.Content(Model.Rows[i]["imgSrc"].ToString())" width="100px" height="100px" />
                </td>
                <td><div id="remove"> <a id="@Model.Rows[i]["DetailsBranch"]" href="javascript:;">حذف</a></div></td>
              </tr>
        }

    </table>
</div>
    <script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js"></script>
<script type="text/javascript" src="http://ajax.cdnjs.com/ajax/libs/json2/20110223/json2.js"></script>
<script>


      $(document).ready(function ()
    {
       $("#remove a").click(function (e) {
           e.preventDefault();
           var id = $(this).attr("id");

           var MSG = confirm("Are you sure you want to delete this Record?");

           if (MSG) {

               $.ajax({
                   type:"POST",
                   url: '/Deleting/Deletebranchdetails/id',
                   data: { id: id },
                   success: function () {
                       $("#row_" + id).remove();
                   },
                   error: function () {

                   }
               });
           }
       })
   });

</script>
}
else { }